using Gtk 4.0;
using Adw 1;

template Gtk.ListItem  {
//  Gtk.ListItem {
    child:
      Adw.Clamp {

        Gtk.Label url_label {
          label: bind (template.item as <$StitchesObject>).url;
        }
      };
//  }
}
